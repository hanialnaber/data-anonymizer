# Environment variables for Data Anonymizer
# Copy this file to .env and update the values as needed

# Windows users: Install make first using:
# choco install make  (if you have Chocolatey)
# or: scoop install make  (if you have Scoop)
# or use Git Bash which includes make

# API Configuration
API_HOST=0.0.0.0
API_PORT=8000

# Streamlit Configuration
STREAMLIT_PORT=8501
STREAMLIT_HOST=localhost

# Backend URL for frontend
BACKEND_URL=http://localhost:8000

# Development settings
PYTHONPATH=src

# Optional: Docker environment indicator
# DOCKER_ENV=true
